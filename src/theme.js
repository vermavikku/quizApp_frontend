// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2', // Your primary color
      light: '#42a5f5', // Your primary light color
    },
    secondary: {
      main: '#dc004e', // Your secondary color
    },
  },
});

export default theme;
